#自底朝上
def mergeSort(data):
    n=len(data)
    temp=[0]*n
    #表示子数组的长度
    for i in range(1,n,2*i):
        for left in range(0,n-i,2*i):
            mid=left+i-1
            right=min(left+2*i-1,n-1)
            self.merge(data,left,mid,right,temp)
def
