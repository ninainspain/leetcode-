from typing import List

class Solution:
    def combinationSum4(self, nums: List[int], target: int) -> int:
        dp = [0] * (target + 1)
        dp[0] = 1
        
        for i in range(target + 1): # Loop through all possible targets up to the main target
            for num in nums:       # Loop through the numbers in nums
                if i - num >= 0 and dp[i] < float('inf') - dp[i - num]: 
                    dp[i] += dp[i - num]
                    
        return dp[target]
