⭕️暴力解法

class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        maxSum=float('-inf')
        for i in range(len(nums)):
            for j in range(i, len(nums)):
                sum = 0
                for k in range(i,j+1):
                    sum+=nums[k]
                maxSum = max(maxSum, sum)
        return maxSum


⭕️dp
状态定义：状态参数 i，j->状态数组dp[i][j]->表示区间[i,j]的累加和

