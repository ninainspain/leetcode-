⭕️第一种情况：当窗口满足条件

left, right = 0,0
#存储结果集
ans=0
while right < len(nums):
    #处理right指向的元素,将其加入到当前窗口中，将中间的结果放到某些数据结构中

    while（窗口满足条件）:
        //可计算结果
        #处理left指向的元素，将其从当前窗口中剔除掉
        left+=1 #直到窗口不满足条件为止
    
    //可计算结果
    right+=1

⭕️第二种情况：当窗口不满足条件

left, right = 0,0

while right < len(nums):
    #处理right指向的元素,将其加入到当前窗口中，将中间的结果放到某些数据结构中

    while（窗口不满足条件）:
        //可计算结果
        #处理left指向的元素，将其从当前窗口中剔除掉
        left+=1 #直到窗口不满足条件为止
    
    //可计算结果
    right+=1
