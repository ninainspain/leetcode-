⭕️直接模拟

class Solution:
    def findKthPositive(self, arr: List[int], k: int) -> int:
        i=0
        currNum=1
        missCnt=0
        lastMissNum=-1
        while missCnt < k:
            if i<len(arr) and arr[i]==currNum:
                i+=1
            else:
                missCnt+=1
                lastMissNum=currNum
            currNum+=1     
        return lastMissNum

 ⭕️
 
 
